import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Brazil_burger',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
